name: NestJS Best Practices CI

on:
  push:
    branches: [main]
    paths:
      - 'skills/nest-best-practices/**'
      - 'packages/nest-best-practices-build/**'
  pull_request:
    branches: [main]
    paths:
      - 'skills/nest-best-practices/**'
      - 'packages/nest-best-practices-build/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/nest-best-practices-build
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.6
      - run: bun install
      - run: bun validate
      - run: bun build